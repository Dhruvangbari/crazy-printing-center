"use client";import{useEffect,useState}from"react";import{useRouter}from"next/navigation";import{supabase}from"../../lib/supabase";export default function Order(){const r=useRouter(),[files,setFiles]=useState([]),[v,setV]=useState({paper_size:"A4",color_mode:"BW",sides:"SINGLE",paper_type:"NORMAL",copies:1,delivery_mode:"PICKUP",address:"",notes:""}),[price,setPrice]=useState(0),[err,setErr]=useState("");useEffect(()=>{let m={A4:1,A5:.8,A3:1.8,Legal:1.2,Letter:1,Custom:1.5}[v.paper_size]||1,b=v.color_mode==="COLOR"?5:1.5;setPrice(Math.max(5,Math.ceil(b*m*(v.sides==="DOUBLE"?.9:1)*Number(v.copies)*Math.max(1,files.length))))},[v,files]);async function go(e){e.preventDefault();const s=supabase(),{data:{user}}=await s.auth.getUser();if(!user)return r.push("/login");if(!files.length)return setErr("Upload at least one document.");const{data:o,error}=await s.from("orders").insert({...v,user_id:user.id,subtotal:price,total:price,status:"ORDER_RECEIVED"}).select().single();if(error)return setErr(error.message);for(const f of files){const path=user.id+"/"+o.id+"/"+Date.now()+"-"+f.name.replace(/[^a-zA-Z0-9._-]/g,"_");let u=await s.storage.from("documents").upload(path,f);if(u.error)return setErr(u.error.message);await s.from("order_files").insert({order_id:o.id,original_name:f.name,storage_path:path,mime_type:f.type,size:f.size})}r.push("/orders/"+o.id)}return <main className="wrap"><div className="card"><h2>New Print Order</h2><form onSubmit={go}><div className="field"><label>Documents</label><input type="file" multiple accept=".pdf,.jpg,.jpeg,.png,.doc,.docx,.ppt,.pptx" onChange={e=>setFiles([...e.target.files])} required/></div><div className="row">{[["paper_size","Paper Size",["A4","A5","A3","Legal","Letter","Custom"]],["color_mode","Colour",["BW","COLOR"]],["sides","Sides",["SINGLE","DOUBLE"]],["paper_type","Paper Type",["NORMAL","PREMIUM","GLOSSY"]],["delivery_mode","Delivery",["PICKUP","DELIVERY"]]].map(([k,l,a])=><div className="field" key={k}><label>{l}</label><select value={v[k]} onChange={e=>setV({...v,[k]:e.target.value})}>{a.map(x=><option key={x}>{x}</option>)}</select></div>)}<div className="field"><label>Copies</label><input type="number" min="1" value={v.copies} onChange={e=>setV({...v,copies:e.target.value})}/></div></div><div className="field"><label>Delivery Address</label><textarea value={v.address} onChange={e=>setV({...v,address:e.target.value})}/></div><div className="field"><label>Notes</label><textarea value={v.notes} onChange={e=>setV({...v,notes:e.target.value})}/></div><p className="price">₹{price}</p>{err&&<p className="error">{err}</p>}<button className="btn">Create Order</button></form></div></main>}