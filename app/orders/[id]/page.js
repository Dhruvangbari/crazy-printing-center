"use client";import{useEffect,useState}from"react";import{useParams}from"next/navigation";import{supabase}from"../../../lib/supabase";export default function Detail(){const p=useParams(),[o,setO]=useState(null),[proof,setProof]=useState(null),[msg,setMsg]=useState("");useEffect(()=>{(async()=>{const{data}=await supabase().from("orders").select("*,order_files(*),status_history(*)").eq("id",p.id).single();setO(data)})()},[p.id]);async function pay(e){e.preventDefault();if(!proof)return;const s=supabase(),path="payment/"+p.id+"-"+Date.now()+"-"+proof.name.replace(/[^a-zA-Z0-9._-]/g,"_");const u=await s.storage.from("payment-proofs").upload(path,proof);if(u.error)return setMsg(u.error.message);const{error}=await s.from("orders").update({payment_proof_path:path,status:"PAYMENT_SUBMITTED"}).eq("id",p.id);setMsg(error?error.message:"Payment screenshot submitted. Host verification is required.");location.reload()}if(!o)return <main className="wrap"><div className="card">Loading...</div></main>;return <main className="wrap"><div className="card"><h2>{o.order_number}</h2><p className="price">₹{o.total}</p><p>{o.paper_size} • {o.color_mode} • {o.sides} • {o.copies} copies</p><h3>Status: <span className="status">{o.status.replaceAll("_"," ")}</span></h3><div className="timeline">{(o.status_history||[]).sort((a,b)=>new Date(a.created_at)-new Date(b.created_at)).map(h=><div className="event" key={h.id}><b>{h.status.replaceAll("_"," ")}</b><div>{new Date(h.created_at).toLocaleString()}</div><div>{h.message}</div></div>)}</div>{!["PAYMENT_VERIFIED","PRINTING","QUALITY_CHECK","READY","OUT_FOR_DELIVERY","DELIVERED"].includes(o.status)&&<div className="card"><h3>UPI Payment</h3><div className="qr">UPI QR<br/><small>{process.env.NEXT_PUBLIC_UPI_ID||"YOUR UPI ID"}</small></div><form onSubmit={pay}><input type="file" accept="image/*" onChange={e=>setProof(e.target.files[0])} required/><button className="btn">Upload Payment Screenshot</button></form>{msg&&<p className="success">{msg}</p>}</div>}</div></main>}